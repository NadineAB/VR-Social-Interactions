using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VRControlCollision : MonoBehaviour
{
   // public Transform handControlMovement;
  //  public Transform headControlMovement;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        Debug.Log("we hit something");
    //    gameObject.transform.position = new Vector3(handControlMovement.position.x, headControlMovement.position.y,
                      //                              handControlMovement.position.z);
        
    }
}
